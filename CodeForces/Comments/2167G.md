# 2167G

You know the idea for this problem is brilliant, just brilliant.

See you look at the numbers you do not change. Since the final sequence is sorted, even this sequence is sorted. So this is a non decreasing subsequence. Now our longest non-decreasing subsequence problem can be modified into heaviest non-decreasing subsequence problem.

Why this works, pick any non - decreasing subsequence then keeping these elements constant we can alter the other elements easily to get a array with no drops. 

So we have non - decreasing subsequence $\iff$ we construct a new array with no drops.

So our problem transforms into finding the heaviest non - decreasing subsequence. 