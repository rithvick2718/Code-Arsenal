# 2167F
Now let $V$ be the vertex set of the graph then for a given $k$, we say 
$$
S_r = \{ v : \exist U \sub V \text{ such that }|U| = k \text{ and } v =\text{LCA}(U) \text{ where the root of the tree is } r \} 
$$
So for a given $k$ we want to find $ \displaystyle \sum_{r = 1}^n |S_r|$

Now my approach to this problem is here as follows :  given a vertex $u$ I find the possible roots $r$ for which $u \in S_r$. 

Now as we are given a tree, every edge is a cut-edge. Consider the edge $(u,v)$ this edge splits the tree into two components say $C_u$ and $C_v$ (it's obvious in which set $u$ belongs). Now take any $r \in C_v$ then $u \in S_r $ if and only if $|C_u| \ge k$.

Why? Visualize. See if $r \in C_v$ and say you have a $U$ such that $\text{LCA}(U) = u$ then $U \sube C_u$, otherwise if the set $U$ has a $s \in C_v$ then the common ancestor of any $t \in C_u$ and $s$ is already greater than $u$. Despite my best efforts this can be better understood once a tree is drawn.

Now let $N(u)$ be the neighbours of $u$ then $\displaystyle 1 + \sum_{v \in N(u) , n - |C_v| \ge k} |C_v|$ is the number of roots $r$ such that $u \in S_r$, the $1$ is as  $ r \in S_r ,\forall r \in V $.

I am not sure if I have convyed my solution clearly, but with no feedback I think this is the best I can do.