# 2182D
What I think : $a[0]$ has a lower bound, if $a[0]$ goes lower than that then we have $0$ permutations, and if $a[0]$ is sufficiently large then all permutations are allowed. Hmm I sense something here.

The upper bound seems to be $0*(a[1] - 0) + 1*(a[2] - a[1]) + 2*(a[3] - a[2]) ... + (n-1)*(a[n] - a[n-1])$. Where $a$ is sorted.

WTH do I mean by upper bound? I mean that if $a[0] \ge$ upper bound then any permutation is allowed.

I found lower bound as $ \text{upper bound} - (n-\text{frequency of largest element}) $, meaning if $a[0]$ is lower than this then then our answer is $0$.

My logic was correct.

See code.